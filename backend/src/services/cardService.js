/**
 * Card Service
 * Business logic for card lifecycle management.
 *
 * Responsibilities:
 *   - issueCard(userId, uid)
 *   - reportLost(cardId)
 *   - reportStolen(cardId)
 *   - revokeCard(cardId, reason)
 *   - replaceCard(oldCardId, newUid)
 *   - handleUserDeparture(userId) — revoke card + deactivate user
 */

// TODO: Implement each function
module.exports = {};
